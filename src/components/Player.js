import React from 'react';
import KeysPlayer from './KeysPlayer';

const Player = () => (
  <KeysPlayer
    haut="up"
    gauche="left"
    bas="down"
    droite="right"
  />
);

export default Player;
